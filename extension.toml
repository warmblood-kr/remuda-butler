name = "butler"
entry = "packages/butler/init.lua"
api = "remuda-lua-v1"
# Routes the documented `remuda butler ...` commands through Remuda's installed subcommand resolver.
command = "butler"

# This manifest describes the target independent-install contract. Resolver
# support and compatibility validation remain responsibilities of Remuda core.
